<!-- Generated by sdk-factory (scripts/generate.sh); DO NOT EDIT. -->

# Account Session

8 operation(s). Part of the [API index](../API.md).

Call these as `await client.<Service>.<Method>Async(...)`. `<service>` is the client field for this
group — the [README](../../README.md) lists the field name for every service.
These pages group operations the way the OpenAPI specification tags them, which
is not always one field per page.

---

## `GetAccessToken`

**GET** `/api/v2/access_token`

Retrieve Access Token

Translates the Request Token obtained during authentication into an Access Token for use with other API calls.

### Example

```c#
public static async Task CallGetAccessToken(LoginRadiusClient client) {
    string token = "<token>"; //Required

    try {
        var response = await client.AccountSession.GetAccessTokenAsync(token);
        Console.WriteLine(response);
    } catch (Exception e) {
        var lr = LoginRadiusException.From(0, null, e);
        Console.Error.WriteLine(lr.Description);
    }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `token` | query | string | yes |  |

### Returns

`AccessTokenResponse`

---

## `GetActiveSession`

**GET** `/api/v2/access_token/activesession`

Retrieve active session

Retrieves details of the current active session for the authenticated User.

### Example

```c#
public static async Task CallGetActiveSession(LoginRadiusClient client) {
    Option<string> token = new Option<string>("<token>"); //Optional
    Option<string> profileid = new Option<string>("<profileid>"); //Optional
    Option<string> accountid = new Option<string>("<accountid>"); //Optional

    try {
        var response = await client.AccountSession.GetActiveSessionAsync(token, profileid, accountid);
        Console.WriteLine(response);
    } catch (Exception e) {
        var lr = LoginRadiusException.From(0, null, e);
        Console.Error.WriteLine(lr.Description);
    }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `token` | query | string | no |  |
| `profileid` | query | string | no | Account ID of the User |
| `accountid` | query | string | no | Account ID of the User |

### Returns

`ActiveSessionResponse`

---

## `NativeInvalidateAccessToken`

**GET** `/api/v2/access_token/invalidate`

Invalidate Access Token

Invalidates the specified Access Token, terminating its validity.

### Example

```c#
public static async Task CallNativeInvalidateAccessToken(LoginRadiusClient client) {
    Option<string> accessToken = new Option<string>("<accessToken>"); //Optional
    Option<bool> preventRefresh = new Option<bool>(true); //Optional

    try {
        var response = await client.AccountSession.NativeInvalidateAccessTokenAsync(accessToken, preventRefresh);
        Console.WriteLine(response);
    } catch (Exception e) {
        var lr = LoginRadiusException.From(0, null, e);
        Console.Error.WriteLine(lr.Description);
    }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `access_token` | query | string | no | Access Token of the User |
| `preventRefresh` | query | boolean | no | Whether to prevent the token from being refreshed (true/false). |

### Returns

`IsPostedResponse`

---

## `NativeRefreshAccessToken`

**GET** `/api/v2/access_token/refresh`

Refresh Access Token

Refreshes the Access Token using a valid Refresh Token to extend session validity. The resulting token lifetime depends on the `expiresin` parameter and the User's registration profile (see the `expiresin` parameter).

### Example

```c#
public static async Task CallNativeRefreshAccessToken(LoginRadiusClient client) {
    Option<string> accessToken = new Option<string>("<accessToken>"); //Optional
    Option<string> isweb = new Option<string>("<isweb>"); //Optional
    Option<int> expiresin = new Option<int>(0); //Optional

    try {
        var response = await client.AccountSession.NativeRefreshAccessTokenAsync(accessToken, isweb, expiresin);
        Console.WriteLine(response);
    } catch (Exception e) {
        var lr = LoginRadiusException.From(0, null, e);
        Console.Error.WriteLine(lr.Description);
    }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `access_token` | query | string | no | Access Token of the User |
| `isweb` | query | string | no | Indicates if the request is from a web client |
| `expiresin` | query | integer (int32) | no | Overrides the default lifetime of the Access Token. The unit and the default applied when this parameter is omitted depend on the User's registration profile: * Email profiles: the value is interpreted in minutes. When omitted, the Access Token uses the application's configured token expiry. * Social login profiles: the value is interpreted in seconds. When omitted, the Access Token adopts the expiry returned by the social provider, falling back to the application's configured token expiry if the provider returns none. |

### Returns

`AccessTokenResponse`

---

## `RefreshAccessToken`

**GET** `/identity/v2/manage/account/access_token/refresh`

Refresh Access Token

Refreshes the Access Token using a Refresh Token.

### Example

```c#
public static async Task CallRefreshAccessToken(LoginRadiusClient client) {
    string refreshToken = "<refreshToken>"; //Required

    try {
        var response = await client.AccountSession.RefreshAccessTokenAsync(refreshToken);
        Console.WriteLine(response);
    } catch (Exception e) {
        var lr = LoginRadiusException.From(0, null, e);
        Console.Error.WriteLine(lr.Description);
    }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `refresh_token` | query | string | yes | Refresh Token |

### Returns

`AccessTokenResponse`

---

## `RevokeAllRefreshToken`

**DELETE** `/identity/v2/manage/account/{uid}/access_token/refresh/revoke`

Revoke refresh tokens

Revokes all active refresh tokens for a specified User.

### Example

```c#
public static async Task CallRevokeAllRefreshToken(LoginRadiusClient client) {
    string uid = "<uid>"; //Required

    try {
        var response = await client.AccountSession.RevokeAllRefreshTokenAsync(uid);
        Console.WriteLine(response);
    } catch (Exception e) {
        var lr = LoginRadiusException.From(0, null, e);
        Console.Error.WriteLine(lr.Description);
    }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `uid` | path | string | yes | The UID associated with the User |

### Returns

`IsDeleted`

---

## `RevokeRefreshToken`

**GET** `/identity/v2/manage/account/access_token/refresh/revoke`

Revoke Refresh Token

Revokes the specified Refresh Token.

### Example

```c#
public static async Task CallRevokeRefreshToken(LoginRadiusClient client) {
    string refreshToken = "<refreshToken>"; //Required

    try {
        var response = await client.AccountSession.RevokeRefreshTokenAsync(refreshToken);
        Console.WriteLine(response);
    } catch (Exception e) {
        var lr = LoginRadiusException.From(0, null, e);
        Console.Error.WriteLine(lr.Description);
    }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `refresh_token` | query | string | yes | Refresh Token |

### Returns

`IsDeleted`

---

## `ValidateAccessToken`

**GET** `/api/v2/access_token/validate`

Validate Access Token

Validates the provided Access Token to ensure its authenticity and validity.

### Example

```c#
public static async Task CallValidateAccessToken(LoginRadiusClient client) {
    string accessToken = "<accessToken>"; //Required

    try {
        var response = await client.AccountSession.ValidateAccessTokenAsync(accessToken);
        Console.WriteLine(response);
    } catch (Exception e) {
        var lr = LoginRadiusException.From(0, null, e);
        Console.Error.WriteLine(lr.Description);
    }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `access_token` | query | string | yes | Access Token of the User |

### Returns

`AccessTokenResponse`

