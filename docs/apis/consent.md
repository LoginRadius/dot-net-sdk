<!-- Generated by the LoginRadius SDK generator; DO NOT EDIT. -->

# Consent

7 operation(s). Part of the [API index](../API.md).

Call these as `await client.<Service>.<Method>Async(...)`. `<service>` is the client field for this
group — the [README](../../README.md) lists the field name for every service.
These pages group operations the way the OpenAPI specification tags them, which
is not always one field per page.

---

## `AddConsentForm`

**POST** `/consent/forms`

Add Consent Form

Adds a new Consent Form for the Tenant.

### Example

```c#
public static async Task CallAddConsentForm(LoginRadiusClient client) {
    ConsentFormModel consentFormModel = new ConsentFormModel("<events>", "<startFromDate>", "<consentOptions>"); //Required

    try {
        var response = await client.Consent.AddConsentFormAsync(consentFormModel);
        Console.WriteLine(response);
    } catch (Exception e) {
        var lr = LoginRadiusException.From(0, null, e);
        Console.Error.WriteLine(lr.Description);
    }
}
```

### Request body

`ConsentFormModel` as `application/json`.

### Returns

`ConsentForm`

---

## `CreateConsentOption`

**POST** `/consent/options`

Create Consent Option

Creates a new consent option for a specific Tenant.

### Example

```c#
public static async Task CallCreateConsentOption(LoginRadiusClient client) {
    ConsentOptionModel consentOptionModel = new ConsentOptionModel("<title>", "<description>"); //Required

    try {
        var response = await client.Consent.CreateConsentOptionAsync(consentOptionModel);
        Console.WriteLine(response);
    } catch (Exception e) {
        var lr = LoginRadiusException.From(0, null, e);
        Console.Error.WriteLine(lr.Description);
    }
}
```

### Request body

`ConsentOptionModel` as `application/json`.

### Returns

`ConsentOptions`

---

## `DeleteConsentForm`

**DELETE** `/consent/forms/{version}`

Delete Consent Form

Deletes the Consent Form identified by the form version for the Tenant.

### Example

```c#
public static async Task CallDeleteConsentForm(LoginRadiusClient client) {
    string version = "<version>"; //Required

    try {
        var response = await client.Consent.DeleteConsentFormAsync(version);
        Console.WriteLine(response);
    } catch (Exception e) {
        var lr = LoginRadiusException.From(0, null, e);
        Console.Error.WriteLine(lr.Description);
    }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `version` | path | string | yes | The version of the Consent form to delete. |

### Returns

`IsDeleted`

---

## `DeleteConsentOption`

**DELETE** `/consent/options/{optionId}`

Delete Consent Option

Deletes the consent option identified by the option ID for the Tenant.

### Example

```c#
public static async Task CallDeleteConsentOption(LoginRadiusClient client) {
    string optionId = "<optionId>"; //Required

    try {
        var response = await client.Consent.DeleteConsentOptionAsync(optionId);
        Console.WriteLine(response);
    } catch (Exception e) {
        var lr = LoginRadiusException.From(0, null, e);
        Console.Error.WriteLine(lr.Description);
    }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `optionId` | path | string | yes | The ID of the Consent option to delete. |

### Returns

`IsDeleted`

---

## `GetActiveConsentForms`

**GET** `/consent/forms/active`

Retrieve Active Consent Forms

Retrieves a list of active Consent Forms configured for the Tenant.

### Example

```c#
public static async Task CallGetActiveConsentForms(LoginRadiusClient client) {
    string varEvent = "<varEvent>"; //Required

    try {
        var response = await client.Consent.GetActiveConsentFormsAsync(varEvent);
        Console.WriteLine(response);
    } catch (Exception e) {
        var lr = LoginRadiusException.From(0, null, e);
        Console.Error.WriteLine(lr.Description);
    }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `event` | query | string | yes | Event type to filter consent verification (e.g., `login`). |

### Returns

`object`

---

## `GetConsentForms`

**GET** `/consent/forms`

Retrieve Consent Forms

Retrieves all Consent Forms configured for the Tenant.

### Example

```c#
public static async Task CallGetConsentForms(LoginRadiusClient client) {
    try {
        var response = await client.Consent.GetConsentFormsAsync();
        Console.WriteLine(response);
    } catch (Exception e) {
        var lr = LoginRadiusException.From(0, null, e);
        Console.Error.WriteLine(lr.Description);
    }
}
```

### Returns

`object`

---

## `GetConsentOptions`

**GET** `/consent/options`

Retrieve Consent Options

Lists all consent options available for a specific Tenant.

### Example

```c#
public static async Task CallGetConsentOptions(LoginRadiusClient client) {
    try {
        var response = await client.Consent.GetConsentOptionsAsync();
        Console.WriteLine(response);
    } catch (Exception e) {
        var lr = LoginRadiusException.From(0, null, e);
        Console.Error.WriteLine(lr.Description);
    }
}
```

### Returns

`object`

