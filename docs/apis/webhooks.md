<!-- Generated by sdk-factory (scripts/generate.sh); DO NOT EDIT. -->

# Webhooks

6 operation(s). Part of the [API index](../API.md).

Call these as `await client.<Service>.<Method>Async(...)`. `<service>` is the client field for this
group — the [README](../../README.md) lists the field name for every service.
These pages group operations the way the OpenAPI specification tags them, which
is not always one field per page.

---

## `CreateWebhookConfiguration`

**POST** `/v2/manage/webhooks`

Create webhook configuration

Creates a new webhook configuration for the Tenant, allowing registration of a webhook with details such as the Target URL and subscribed events.

### Example

```c#
public static async Task CallCreateWebhookConfiguration(LoginRadiusClient client) {
    WebhookSubscriptionCreateModel webhookSubscriptionCreateModel = new WebhookSubscriptionCreateModel("<targetUrl>"); //Optional

    try {
        var response = await client.Webhooks.CreateWebhookConfigurationAsync(webhookSubscriptionCreateModel);
        Console.WriteLine(response);
    } catch (Exception e) {
        var lr = LoginRadiusException.From(0, null, e);
        Console.Error.WriteLine(lr.Description);
    }
}
```

### Request body

`WebhookSubscriptionCreateModel` as `application/json`.

### Returns

`WebhookSubscription`

---

## `DeleteWebhookConfigurationById`

**DELETE** `/v2/manage/webhooks/{hookId}`

Delete webhook configuration

Deletes a specific webhook configuration for the Tenant using its unique ID, permanently removing the webhook from receiving further event notifications.

### Example

```c#
public static async Task CallDeleteWebhookConfigurationById(LoginRadiusClient client) {
    string hookId = "<hookId>"; //Required

    try {
        var response = await client.Webhooks.DeleteWebhookConfigurationByIdAsync(hookId);
        Console.WriteLine(response);
    } catch (Exception e) {
        var lr = LoginRadiusException.From(0, null, e);
        Console.Error.WriteLine(lr.Description);
    }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `hookId` | path | string | yes | Webhook ID |

### Returns

`DeleteResponse`

---

## `GetAllEvents`

**GET** `/v2/manage/webhooks/events`

List webhook events

Retrieves a list of all available webhook events that can be subscribed to by the Tenant for configuring webhooks to receive notifications for specific activities.

### Example

```c#
public static async Task CallGetAllEvents(LoginRadiusClient client) {
    try {
        var response = await client.Webhooks.GetAllEventsAsync();
        Console.WriteLine(response);
    } catch (Exception e) {
        var lr = LoginRadiusException.From(0, null, e);
        Console.Error.WriteLine(lr.Description);
    }
}
```

### Returns

`WebhookEvents`

---

## `GetAllWebhooksConfigurations`

**GET** `/v2/manage/webhooks`

List webhook configurations

Retrieves a list of all configured webhooks for the Tenant, including detailed information about each webhook and its subscribed events.

### Example

```c#
public static async Task CallGetAllWebhooksConfigurations(LoginRadiusClient client) {
    try {
        var response = await client.Webhooks.GetAllWebhooksConfigurationsAsync();
        Console.WriteLine(response);
    } catch (Exception e) {
        var lr = LoginRadiusException.From(0, null, e);
        Console.Error.WriteLine(lr.Description);
    }
}
```

### Returns

`WebhookSubscriptionResponse`

---

## `GetWebhookConfigurationById`

**GET** `/v2/manage/webhooks/{hookId}`

Retrieve webhook configuration

Retrieves the details of a specific webhook configuration for the Tenant by its unique ID, including the Target URL, subscribed events, and other settings.

### Example

```c#
public static async Task CallGetWebhookConfigurationById(LoginRadiusClient client) {
    string hookId = "<hookId>"; //Required

    try {
        var response = await client.Webhooks.GetWebhookConfigurationByIdAsync(hookId);
        Console.WriteLine(response);
    } catch (Exception e) {
        var lr = LoginRadiusException.From(0, null, e);
        Console.Error.WriteLine(lr.Description);
    }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `hookId` | path | string | yes | Webhook ID |

### Returns

`WebhookSubscription`

---

## `UpdateWebhookConfigurationById`

**PUT** `/v2/manage/webhooks/{hookId}`

Update webhook configuration

Updates an existing webhook configuration for the Tenant by its unique ID, modifying details such as the Target URL, subscribed events, or other settings.

### Example

```c#
public static async Task CallUpdateWebhookConfigurationById(LoginRadiusClient client) {
    string hookId = "<hookId>"; //Required
    WebhookSubscriptionUpdateModel webhookSubscriptionUpdateModel = new WebhookSubscriptionUpdateModel("<targetUrl>"); //Optional

    try {
        var response = await client.Webhooks.UpdateWebhookConfigurationByIdAsync(hookId, webhookSubscriptionUpdateModel);
        Console.WriteLine(response);
    } catch (Exception e) {
        var lr = LoginRadiusException.From(0, null, e);
        Console.Error.WriteLine(lr.Description);
    }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `hookId` | path | string | yes | Webhook ID |

### Request body

`WebhookSubscriptionUpdateModel` as `application/json`.

### Returns

`WebhookSubscription`

