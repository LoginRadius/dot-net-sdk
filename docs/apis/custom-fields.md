<!-- Generated by the LoginRadius SDK generator; DO NOT EDIT. -->

# Custom Fields

7 operation(s). Part of the [API index](../API.md).

Call these as `await client.<Service>.<Method>Async(...)`. `<service>` is the client field for this
group — the [README](../../README.md) lists the field name for every service.
These pages group operations the way the OpenAPI specification tags them, which
is not always one field per page.

---

## `CreateCustomField`

**POST** `/v2/manage/custom-fields`

Create custom field

Creates a new Custom Field for the Tenant.

### Example

```c#
public static async Task CallCreateCustomField(LoginRadiusClient client) {
    RaasCustomFieldModel raasCustomFieldModel = new RaasCustomFieldModel("<customField>"); //Required

    try {
        var response = await client.CustomFields.CreateCustomFieldAsync(raasCustomFieldModel);
        Console.WriteLine(response);
    } catch (Exception e) {
        var lr = LoginRadiusException.From(0, null, e);
        Console.Error.WriteLine(lr.Description);
    }
}
```

### Request body

`RaasCustomFieldModel` as `application/json`.

### Returns

`RaasCustomField`

---

## `DeleteCustomField`

**DELETE** `/v2/manage/custom-fields/{cfname}`

Delete custom field

Deletes a Custom Field by name for the Tenant.

### Example

```c#
public static async Task CallDeleteCustomField(LoginRadiusClient client) {
    string cfname = "<cfname>"; //Required

    try {
        var response = await client.CustomFields.DeleteCustomFieldAsync(cfname);
        Console.WriteLine(response);
    } catch (Exception e) {
        var lr = LoginRadiusException.From(0, null, e);
        Console.Error.WriteLine(lr.Description);
    }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `cfname` | path | string | yes | Custom Fields Name |

### Returns

`DeleteResponse`

---

## `GetActiveCustomFields`

**GET** `/v2/manage/custom-fields/active`

List active custom fields

Retrieves all custom fields currently active in the registration form for the Tenant.

### Example

```c#
public static async Task CallGetActiveCustomFields(LoginRadiusClient client) {
    try {
        var response = await client.CustomFields.GetActiveCustomFieldsAsync();
        Console.WriteLine(response);
    } catch (Exception e) {
        var lr = LoginRadiusException.From(0, null, e);
        Console.Error.WriteLine(lr.Description);
    }
}
```

### Returns

`object`

---

## `GetAllCustomFields`

**GET** `/v2/manage/custom-fields`

List custom fields

Retrieves all Custom Fields created for the Tenant.

### Example

```c#
public static async Task CallGetAllCustomFields(LoginRadiusClient client) {
    try {
        var response = await client.CustomFields.GetAllCustomFieldsAsync();
        Console.WriteLine(response);
    } catch (Exception e) {
        var lr = LoginRadiusException.From(0, null, e);
        Console.Error.WriteLine(lr.Description);
    }
}
```

### Returns

`object`

---

## `GetCustomFieldLimit`

**GET** `/v2/manage/custom-fields/limit`

Retrieve custom field limit

Retrieves the Custom Field Limit configured for the Tenant.

### Example

```c#
public static async Task CallGetCustomFieldLimit(LoginRadiusClient client) {
    try {
        var response = await client.CustomFields.GetCustomFieldLimitAsync();
        Console.WriteLine(response);
    } catch (Exception e) {
        var lr = LoginRadiusException.From(0, null, e);
        Console.Error.WriteLine(lr.Description);
    }
}
```

### Returns

`CustomFieldLimitResponse`

---

## `ListCustomFields`

**GET** `/v2/manage/custom-fields/list`

List custom fields

Retrieves all custom fields for the Tenant, returned as an array of strings.

### Example

```c#
public static async Task CallListCustomFields(LoginRadiusClient client) {
    try {
        var response = await client.CustomFields.ListCustomFieldsAsync();
        Console.WriteLine(response);
    } catch (Exception e) {
        var lr = LoginRadiusException.From(0, null, e);
        Console.Error.WriteLine(lr.Description);
    }
}
```

### Returns

`object`

---

## `SetCustomField`

**PUT** `/v2/manage/custom-fields`

Set custom field

Updates or sets a Custom Field instance in RAAS to be displayed on forms for the Tenant.

### Example

```c#
public static async Task CallSetCustomField(LoginRadiusClient client) {
    SetCustomFieldRequest setCustomFieldRequest = new SetCustomFieldRequest("<data>"); //Required

    try {
        var response = await client.CustomFields.SetCustomFieldAsync(setCustomFieldRequest);
        Console.WriteLine(response);
    } catch (Exception e) {
        var lr = LoginRadiusException.From(0, null, e);
        Console.Error.WriteLine(lr.Description);
    }
}
```

### Request body

`object` as `application/json`.

### Returns

`object`

