<!-- Generated by the LoginRadius SDK generator; DO NOT EDIT. -->

# Password Policy

2 operation(s). Part of the [API index](../API.md).

Call these as `await client.<Service>.<Method>Async(...)`. `<service>` is the client field for this
group — the [README](../../README.md) lists the field name for every service.
These pages group operations the way the OpenAPI specification tags them, which
is not always one field per page.

---

## `GetPasswordPolicy`

**GET** `/v2/manage/password-policies`

Retrieve Password policy

Retrieves the Password policy settings for a specific Tenant.

### Example

```c#
public static async Task CallGetPasswordPolicy(LoginRadiusClient client) {
    try {
        var response = await client.PasswordPolicy.GetPasswordPolicyAsync();
        Console.WriteLine(response);
    } catch (Exception e) {
        var lr = LoginRadiusException.From(0, null, e);
        Console.Error.WriteLine(lr.Description);
    }
}
```

### Returns

`PasswordPolicy`

---

## `UpdatePasswordPolicy`

**PUT** `/v2/manage/password-policies`

Update Password policy

Updates the Password policy settings for a specific Tenant.

### Example

```c#
public static async Task CallUpdatePasswordPolicy(LoginRadiusClient client) {
    PasswordPolicy passwordPolicy = new PasswordPolicy("<dictionaryPasswordValidation>", "<profileDataPasswordValidation>", "<profileDataPasswordExactMatch>"); //Required

    try {
        var response = await client.PasswordPolicy.UpdatePasswordPolicyAsync(passwordPolicy);
        Console.WriteLine(response);
    } catch (Exception e) {
        var lr = LoginRadiusException.From(0, null, e);
        Console.Error.WriteLine(lr.Description);
    }
}
```

### Request body

`PasswordPolicy` as `application/json`.

### Returns

`PasswordPolicy`

