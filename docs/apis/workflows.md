<!-- Generated by the LoginRadius SDK generator; DO NOT EDIT. -->

# Workflows

8 operation(s). Part of the [API index](../API.md).

Call these as `await client.<Service>.<Method>Async(...)`. `<service>` is the client field for this
group — the [README](../../README.md) lists the field name for every service.
These pages group operations the way the OpenAPI specification tags them, which
is not always one field per page.

---

## `AddWorkflow`

**POST** `/v2/manage/workflows`

Add workflow

Adds a new workflow configuration to the Tenant.

### Example

```c#
public static async Task CallAddWorkflow(LoginRadiusClient client) {
    AddWorkflowConfig addWorkflowConfig = new AddWorkflowConfig("<name>", "<data>"); //Required

    try {
        var response = await client.Workflows.AddWorkflowAsync(addWorkflowConfig);
        Console.WriteLine(response);
    } catch (Exception e) {
        var lr = LoginRadiusException.From(0, null, e);
        Console.Error.WriteLine(lr.Description);
    }
}
```

### Request body

`AddWorkflowConfig` as `application/json`.

### Returns

`WorkflowConfig`

---

## `DeleteWorkflow`

**DELETE** `/v2/manage/workflows/{workflowId}`

Delete Workflow

Deletes an existing Workflow from the system.

### Example

```c#
public static async Task CallDeleteWorkflow(LoginRadiusClient client) {
    string workflowId = "<workflowId>"; //Required

    try {
        var response = await client.Workflows.DeleteWorkflowAsync(workflowId);
        Console.WriteLine(response);
    } catch (Exception e) {
        var lr = LoginRadiusException.From(0, null, e);
        Console.Error.WriteLine(lr.Description);
    }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `workflowId` | path | string | yes | The ID of the workflow. |

### Returns

`DeleteResponse`

---

## `DeleteWorkflowVersion`

**DELETE** `/v2/manage/workflows/{workflowId}/versions/{version}`

Delete Workflow Version

Deletes a specific version of a Workflow from the system.

### Example

```c#
public static async Task CallDeleteWorkflowVersion(LoginRadiusClient client) {
    string workflowId = "<workflowId>"; //Required
    string version = "<version>"; //Required

    try {
        var response = await client.Workflows.DeleteWorkflowVersionAsync(workflowId, version);
        Console.WriteLine(response);
    } catch (Exception e) {
        var lr = LoginRadiusException.From(0, null, e);
        Console.Error.WriteLine(lr.Description);
    }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `workflowId` | path | string | yes | The ID of the workflow. |
| `version` | path | string | yes | The version identifier to delete. |

### Returns

`IsDeleted`

---

## `GetAllWorkflows`

**GET** `/v2/manage/workflows`

List workflows

Retrieves a list of all workflows configured for the Tenant.

### Example

```c#
public static async Task CallGetAllWorkflows(LoginRadiusClient client) {
    try {
        var response = await client.Workflows.GetAllWorkflowsAsync();
        Console.WriteLine(response);
    } catch (Exception e) {
        var lr = LoginRadiusException.From(0, null, e);
        Console.Error.WriteLine(lr.Description);
    }
}
```

### Returns

`object`

---

## `GetAllWorkflowVersionList`

**GET** `/v2/manage/workflows/{workflowId}/versions`

List Workflow Versions

Returns a list of all available versions for a specified Workflow.

### Example

```c#
public static async Task CallGetAllWorkflowVersionList(LoginRadiusClient client) {
    string workflowId = "<workflowId>"; //Required

    try {
        var response = await client.Workflows.GetAllWorkflowVersionListAsync(workflowId);
        Console.WriteLine(response);
    } catch (Exception e) {
        var lr = LoginRadiusException.From(0, null, e);
        Console.Error.WriteLine(lr.Description);
    }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `workflowId` | path | string | yes | The ID of the workflow. |

### Returns

`VersionListResponse`

---

## `GetWorkflowById`

**GET** `/v2/manage/workflows/{workflowId}`

Retrieve Workflow

Retrieves details of a specific Workflow using its unique identifier.

### Example

```c#
public static async Task CallGetWorkflowById(LoginRadiusClient client) {
    string workflowId = "<workflowId>"; //Required

    try {
        var response = await client.Workflows.GetWorkflowByIdAsync(workflowId);
        Console.WriteLine(response);
    } catch (Exception e) {
        var lr = LoginRadiusException.From(0, null, e);
        Console.Error.WriteLine(lr.Description);
    }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `workflowId` | path | string | yes | The ID of the workflow. |

### Returns

`WorkflowConfig`

---

## `RestoreWorkflowVersion`

**PUT** `/v2/manage/workflows/{workflowId}/versions/{version}`

Restore Workflow Version

Restores a specific version of a Workflow to its active state.

### Example

```c#
public static async Task CallRestoreWorkflowVersion(LoginRadiusClient client) {
    string workflowId = "<workflowId>"; //Required
    string version = "<version>"; //Required

    try {
        var response = await client.Workflows.RestoreWorkflowVersionAsync(workflowId, version);
        Console.WriteLine(response);
    } catch (Exception e) {
        var lr = LoginRadiusException.From(0, null, e);
        Console.Error.WriteLine(lr.Description);
    }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `workflowId` | path | string | yes | The ID of the workflow. |
| `version` | path | string | yes | The version identifier to delete. |

### Returns

`object`

---

## `UpdateWorkflow`

**PUT** `/v2/manage/workflows/{workflowId}`

Update Workflow

Updates the configuration of an existing Workflow.

### Example

```c#
public static async Task CallUpdateWorkflow(LoginRadiusClient client) {
    string workflowId = "<workflowId>"; //Required
    UpdateWorkflowConfig updateWorkflowConfig = new UpdateWorkflowConfig("<name>", "<themeName>", "<description>"); //Required

    try {
        var response = await client.Workflows.UpdateWorkflowAsync(workflowId, updateWorkflowConfig);
        Console.WriteLine(response);
    } catch (Exception e) {
        var lr = LoginRadiusException.From(0, null, e);
        Console.Error.WriteLine(lr.Description);
    }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `workflowId` | path | string | yes | The ID of the workflow. |

### Request body

`UpdateWorkflowConfig` as `application/json`.

### Returns

`WorkflowConfig`

