<!-- Generated by the LoginRadius SDK generator; DO NOT EDIT. -->

# Organization Domains

5 operation(s). Part of the [API index](../API.md).

Call these as `await client.<Service>.<Method>Async(...)`. `<service>` is the client field for this
group — the [README](../../README.md) lists the field name for every service.
These pages group operations the way the OpenAPI specification tags them, which
is not always one field per page.

---

## `AddOrganizationDomain`

**POST** `/v2/manage/organizations/{orgId}/domains`

Add Organization domain

Adds a new domain to an Organization.

### Example

```c#
public static async Task CallAddOrganizationDomain(LoginRadiusClient client) {
    string orgId = "<orgId>"; //Required
    AddOrganizationDomainRequest addOrganizationDomainRequest = new AddOrganizationDomainRequest("<domainName>"); //Required

    try {
        var response = await client.OrganizationDomains.AddOrganizationDomainAsync(orgId, addOrganizationDomainRequest);
        Console.WriteLine(response);
    } catch (Exception e) {
        var lr = LoginRadiusException.From(0, null, e);
        Console.Error.WriteLine(lr.Description);
    }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `orgId` | path | string | yes | Organization ID |

### Request body

`object` as `application/json`.

### Returns

`OrganizationsDomainsResponse`

---

## `DeleteOrganizationDomain`

**DELETE** `/v2/manage/organizations/{orgId}/domains/{domainId}`

Delete Organization domain

Deletes a domain from an Organization.

### Example

```c#
public static async Task CallDeleteOrganizationDomain(LoginRadiusClient client) {
    string orgId = "<orgId>"; //Required
    string domainId = "<domainId>"; //Required

    try {
        var response = await client.OrganizationDomains.DeleteOrganizationDomainAsync(orgId, domainId);
        Console.WriteLine(response);
    } catch (Exception e) {
        var lr = LoginRadiusException.From(0, null, e);
        Console.Error.WriteLine(lr.Description);
    }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `orgId` | path | string | yes | Organization ID |
| `domainId` | path | string | yes | Organization Domain ID |

### Returns

`DeleteResponse`

---

## `GetAllOrganizationDomains`

**GET** `/v2/manage/organizations/{orgId}/domains`

List Organization domains

Lists all domains associated with an Organization.

### Example

```c#
public static async Task CallGetAllOrganizationDomains(LoginRadiusClient client) {
    string orgId = "<orgId>"; //Required

    try {
        var response = await client.OrganizationDomains.GetAllOrganizationDomainsAsync(orgId);
        Console.WriteLine(response);
    } catch (Exception e) {
        var lr = LoginRadiusException.From(0, null, e);
        Console.Error.WriteLine(lr.Description);
    }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `orgId` | path | string | yes | Organization ID |

### Returns

`object`

---

## `GetOrganizationDomain`

**GET** `/v2/manage/organizations/{orgId}/domains/{domainId}`

Retrieve Organization domain

Retrieves details of a specific Organization domain.

### Example

```c#
public static async Task CallGetOrganizationDomain(LoginRadiusClient client) {
    string orgId = "<orgId>"; //Required
    string domainId = "<domainId>"; //Required

    try {
        var response = await client.OrganizationDomains.GetOrganizationDomainAsync(orgId, domainId);
        Console.WriteLine(response);
    } catch (Exception e) {
        var lr = LoginRadiusException.From(0, null, e);
        Console.Error.WriteLine(lr.Description);
    }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `orgId` | path | string | yes | Organization ID |
| `domainId` | path | string | yes | Organization Domain ID |

### Returns

`OrganizationsDomainsResponse`

---

## `VerifyOrganizationDomain`

**POST** `/v2/manage/organizations/{orgId}/domains/{domainId}`

Verify Organization domain

Verifies the ownership of an Organization domain.

### Example

```c#
public static async Task CallVerifyOrganizationDomain(LoginRadiusClient client) {
    string domainId = "<domainId>"; //Required
    string orgId = "<orgId>"; //Required

    try {
        var response = await client.OrganizationDomains.VerifyOrganizationDomainAsync(domainId, orgId);
        Console.WriteLine(response);
    } catch (Exception e) {
        var lr = LoginRadiusException.From(0, null, e);
        Console.Error.WriteLine(lr.Description);
    }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `domainId` | path | string | yes | Organization Domain ID |
| `orgId` | path | string | yes | Organization ID |

### Returns

`OrganizationsDomainsResponse`

