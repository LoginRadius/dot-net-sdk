<!-- Generated by the LoginRadius SDK generator; DO NOT EDIT. -->

# Permissions

5 operation(s). Part of the [API index](../API.md).

Call these as `await client.<Service>.<Method>Async(...)`. `<service>` is the client field for this
group — the [README](../../README.md) lists the field name for every service.
These pages group operations the way the OpenAPI specification tags them, which
is not always one field per page.

---

## `AddPermission`

**POST** `/v2/manage/permissions`

Create Permission

Adds a new Permission.

### Example

```c#
public static async Task CallAddPermission(LoginRadiusClient client) {
    PermissionsPostRequest permissionsPostRequest = new PermissionsPostRequest("<name>"); //Optional

    try {
        var response = await client.Permissions.AddPermissionAsync(permissionsPostRequest);
        Console.WriteLine(response);
    } catch (Exception e) {
        var lr = LoginRadiusException.From(0, null, e);
        Console.Error.WriteLine(lr.Description);
    }
}
```

### Request body

`PermissionsPostRequest` as `application/json`.

### Returns

`Permissions`

---

## `DeleteTenantPermission`

**DELETE** `/v2/manage/permissions/{id}`

Delete Permission

Deletes a specific Permission.

### Example

```c#
public static async Task CallDeleteTenantPermission(LoginRadiusClient client) {
    string id = "<id>"; //Required

    try {
        var response = await client.Permissions.DeleteTenantPermissionAsync(id);
        Console.WriteLine(response);
    } catch (Exception e) {
        var lr = LoginRadiusException.From(0, null, e);
        Console.Error.WriteLine(lr.Description);
    }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `id` | path | string | yes | The unique identifier for the Permission |

### Returns

`DeleteResponse`

---

## `GetPermissionById`

**GET** `/v2/manage/permissions/{id}`

Retrieve Permission by ID

Retrieves a Permission by its ID.

### Example

```c#
public static async Task CallGetPermissionById(LoginRadiusClient client) {
    string id = "<id>"; //Required

    try {
        var response = await client.Permissions.GetPermissionByIdAsync(id);
        Console.WriteLine(response);
    } catch (Exception e) {
        var lr = LoginRadiusException.From(0, null, e);
        Console.Error.WriteLine(lr.Description);
    }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `id` | path | string | yes | The unique identifier for the Permission |

### Returns

`Permissions`

---

## `Permissions`

**GET** `/v2/manage/permissions`

List Permissions

Retrieves a list of all Permissions.

### Example

```c#
public static async Task CallPermissions(LoginRadiusClient client) {
    try {
        var response = await client.Permissions.PermissionsAsync();
        Console.WriteLine(response);
    } catch (Exception e) {
        var lr = LoginRadiusException.From(0, null, e);
        Console.Error.WriteLine(lr.Description);
    }
}
```

### Returns

`object`

---

## `UpdateTenantPermission`

**PUT** `/v2/manage/permissions/{id}`

Update Permission

Updates a specific Permission. Note: The Name field cannot be modified for non-B2B apps. If a different Name value is provided, the API will return an error.

### Example

```c#
public static async Task CallUpdateTenantPermission(LoginRadiusClient client) {
    string id = "<id>"; //Required
    PermissionPutRequest permissionPutRequest = new PermissionPutRequest("<name>", "<description>"); //Optional

    try {
        var response = await client.Permissions.UpdateTenantPermissionAsync(id, permissionPutRequest);
        Console.WriteLine(response);
    } catch (Exception e) {
        var lr = LoginRadiusException.From(0, null, e);
        Console.Error.WriteLine(lr.Description);
    }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `id` | path | string | yes | The unique identifier for the Permission |

### Request body

`PermissionPutRequest` as `application/json`.

### Returns

`Permissions`

